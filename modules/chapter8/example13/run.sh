#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")"
echo "Running Example 8.13 — Documentation Depth by Intended Use"
cat <<'EOF'
- Intended use and audience determine documentation depth.
- Training data needs enough detail to reproduce features, labels, and preprocessing.
EOF
