#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")"
echo "Running Example 8.23 — DataHub for Real-Time Feed Versions"
cat <<'EOF'
- DataHub connects evolving feeds to metadata, lineage, and ownership.
- Cross-team users can discover and interpret each documented version.
EOF
