#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")"
echo "Running Example 8.7 — Gender Allowed Values"
cat <<'EOF'
- Categorical fields need an explicit controlled vocabulary.
- Allowed values enable validation and consistent analysis.
EOF
