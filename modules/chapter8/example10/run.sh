#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")"
echo "Running Example 8.10 — Retail Transaction Provenance Trail"
cat <<'EOF'
- Lineage connects point-of-sale source, deduplication, and API enrichment.
- Every transformation affects how users judge integrity and validity.
EOF
