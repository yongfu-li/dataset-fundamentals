#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")"
echo "Running Example 8.5 — Air-Quality Metadata Fields"
cat <<'EOF'
- Record creator, collection period, cadence, and external sources.
- These fields establish accountability, timeliness, and data authenticity.
EOF
