#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")"
echo "Running Example 8.11 — Urban-Only Sampling Note"
cat <<'EOF'
- Urban-only collection is a sampling limitation.
- The dataset must not be assumed representative of rural populations.
EOF
