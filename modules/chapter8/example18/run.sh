#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")"
echo "Running Example 8.18 — Interoperable Schema for Dataset Linking"
cat <<'EOF'
- Standard schemas let systems interpret fields consistently.
- Linking context makes independently published datasets interoperable.
EOF
