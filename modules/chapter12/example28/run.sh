#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")"

echo "Running Example 12.28 — InfluxDB for Timestamped Metrics"
cat <<'EOF'
InfluxDB / time-series:
- High-rate sensor and market ticks
- Real-time monitoring and rollups
- Purpose-built for timestamped metrics
EOF
