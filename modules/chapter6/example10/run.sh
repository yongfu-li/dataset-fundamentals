#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")"
echo "Running Example 6.10 — Continuous Physical Measures"
cat <<'EOF'
Continuous measures:
Examples: height, weight, temperature, time.
They may take decimal values within a range (for example, 175.2 cm) and are measured rather than counted.
EOF
