#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")"

echo "Running Example 13.41 — RMarkdown Public Health Analysis Report"
cat <<'EOF'
RMarkdown: recompile to keep code and narrative in sync.
EOF
