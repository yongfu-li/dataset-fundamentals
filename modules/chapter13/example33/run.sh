#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")"

echo "Running Example 13.33 — Notebook Scale and Environment Limits"
cat <<'EOF'
Notebook limits: pair with version control and containers.
EOF
