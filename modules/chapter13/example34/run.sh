#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")"

echo "Running Example 13.34 — Docker Image for Sequence Alignment Tools"
cat <<'EOF'
Docker: pin exact tool versions in a reproducible image.
EOF
