#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")"

echo "Running Example 13.10 — Repository Metadata Enables Discovery"
cat <<'EOF'
Standards-compliant metadata -> discoverable, reusable datasets.
EOF
