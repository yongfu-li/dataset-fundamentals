#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")"

echo "Running Example 13.12 — Indexing Genomics Data in Public Archives"
cat <<'EOF'
Findable: deposit in indexed archives with persistent identifiers.
EOF
