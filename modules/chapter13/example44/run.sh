#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")"

echo "Running Example 13.44 — DVC Tracks Training Data and Weights"
cat <<'EOF'
DVC: version training data, weights, and evaluation outputs.
EOF
