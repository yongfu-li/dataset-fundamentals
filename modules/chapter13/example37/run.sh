#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")"

echo "Running Example 13.37 — CI Checks Preprocessing and Inputs"
cat <<'EOF'
CI: auto-test preprocessing, dependencies, and input formats.
EOF
