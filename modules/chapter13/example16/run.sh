#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")"

echo "Running Example 13.16 — Linking Expression Data to Ensembl"
cat <<'EOF'
Interoperable: link datasets to external references (Ensembl, UniProt).
EOF
